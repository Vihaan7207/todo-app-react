import './TodoList.css';

export default function TodoList() {
    return (
        <div>
            <input></input>
            <button onClick={() => console.log('click')}>Add</button>
        </div>
    );
}